export type EventType = {
    id: number,
    title: string,
    date: string,
    time: string,
    description: string
}
export type Achievement = {
    id: number,
    name: string,
    description: string,
    image: any
}